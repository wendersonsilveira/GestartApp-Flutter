import 'package:flutter/material.dart';

class Gerar_Codigo extends StatelessWidget {
  const Gerar_Codigo({
    Key key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('wewew');
  }
}
