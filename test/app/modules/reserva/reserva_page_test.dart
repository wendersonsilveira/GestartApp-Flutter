import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:Gestart/app/modules/reserva/reserva_page.dart';

void main() {
  testWidgets('ReservaPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ReservaPage(title: 'Reserva')));
    //  final titleFinder = find.text('Reserva');
    //  expect(titleFinder, findsOneWidget);
  });
}
