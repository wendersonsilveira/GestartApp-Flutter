import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:Gestart/app/modules/reserva/dados_reserva/dados_reserva_page.dart';

void main() {
  testWidgets('DadosReservaPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(DadosReservaPage(title: 'DadosReserva')));
    //  final titleFinder = find.text('DadosReserva');
    //  expect(titleFinder, findsOneWidget);
  });
}
