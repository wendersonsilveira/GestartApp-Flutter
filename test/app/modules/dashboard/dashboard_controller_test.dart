import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:Gestart/app/modules/dashboard/dashboard_controller.dart';
import 'package:Gestart/app/app_module.dart';

void main() {
  initModule(AppModule());
  // DashboardController dashboard;
  //
  setUp(() {
    //     dashboard = AppModule.to.get<DashboardController>();
  });

  group('DashboardController Test', () {
    //   test("First Test", () {
    //     expect(dashboard, isInstanceOf<DashboardController>());
    //   });

    //   test("Set Value", () {
    //     expect(dashboard.value, equals(0));
    //     dashboard.increment();
    //     expect(dashboard.value, equals(1));
    //   });
  });
}
