import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:Gestart/app/modules/balancete/balancete_page.dart';

void main() {
  testWidgets('BalancetePage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(BalancetePage(title: 'Balancete')));
    //  final titleFinder = find.text('Balancete');
    //  expect(titleFinder, findsOneWidget);
  });
}
