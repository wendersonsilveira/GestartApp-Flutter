import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:Gestart/app/modules/documentos/documentos_page.dart';

void main() {
  testWidgets('DocumentosPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(DocumentosPage(title: 'Documentos')));
    //  final titleFinder = find.text('Documentos');
    //  expect(titleFinder, findsOneWidget);
  });
}
