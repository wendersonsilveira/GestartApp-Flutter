import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:Gestart/app/modules/sindico/sindico_controller.dart';
import 'package:Gestart/app/app_module.dart';

void main() {
  initModule(AppModule());
  // SindicoController sindico;
  //
  setUp(() {
    //     sindico = AppModule.to.get<SindicoController>();
  });

  group('SindicoController Test', () {
    //   test("First Test", () {
    //     expect(sindico, isInstanceOf<SindicoController>());
    //   });

    //   test("Set Value", () {
    //     expect(sindico.value, equals(0));
    //     sindico.increment();
    //     expect(sindico.value, equals(1));
    //   });
  });
}
