import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:Gestart/app/modules/sindico/financeiro/movimento_financeiro/detalhe_movimento/detalhe_movimento_controller.dart';
import 'package:Gestart/app/app_module.dart';

void main() {
  initModule(AppModule());
  // DetalheMovimentoController detalhemovimento;
  //
  setUp(() {
    //     detalhemovimento = AppModule.to.get<DetalheMovimentoController>();
  });

  group('DetalheMovimentoController Test', () {
    //   test("First Test", () {
    //     expect(detalhemovimento, isInstanceOf<DetalheMovimentoController>());
    //   });

    //   test("Set Value", () {
    //     expect(detalhemovimento.value, equals(0));
    //     detalhemovimento.increment();
    //     expect(detalhemovimento.value, equals(1));
    //   });
  });
}
