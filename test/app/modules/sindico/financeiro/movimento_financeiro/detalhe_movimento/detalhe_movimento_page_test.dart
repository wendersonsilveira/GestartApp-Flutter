import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('DetalheMovimentoPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(DetalheMovimentoPage(title: 'DetalheMovimento')));
    //  final titleFinder = find.text('DetalheMovimento');
    //  expect(titleFinder, findsOneWidget);
  });
}
