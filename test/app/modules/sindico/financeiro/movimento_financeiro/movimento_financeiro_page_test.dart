import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('MovimentoFinanceiroPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(MovimentoFinanceiroPage(title: 'MovimentoFinanceiro')));
    //  final titleFinder = find.text('MovimentoFinanceiro');
    //  expect(titleFinder, findsOneWidget);
  });
}
