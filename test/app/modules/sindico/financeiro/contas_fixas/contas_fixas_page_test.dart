import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:Gestart/app/modules/sindico/financeiro/contas_fixas/contas_fixas_page.dart';

void main() {
  testWidgets('ContasFixasPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ContasFixasPage(title: 'ContasFixas')));
    //  final titleFinder = find.text('ContasFixas');
    //  expect(titleFinder, findsOneWidget);
  });
}
