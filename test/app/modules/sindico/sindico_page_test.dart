import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:Gestart/app/modules/sindico/sindico_page.dart';

void main() {
  testWidgets('SindicoPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(SindicoPage(title: 'Sindico')));
    //  final titleFinder = find.text('Sindico');
    //  expect(titleFinder, findsOneWidget);
  });
}
