import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:Gestart/app/modules/sindico/gerenciar_reserva/reservas_pendentes/reservas_pendentes_controller.dart';
import 'package:Gestart/app/app_module.dart';

void main() {
  initModule(AppModule());
  // ReservasPendentesController reservaspendentes;
  //
  setUp(() {
    //     reservaspendentes = AppModule.to.get<ReservasPendentesController>();
  });

  group('ReservasPendentesController Test', () {
    //   test("First Test", () {
    //     expect(reservaspendentes, isInstanceOf<ReservasPendentesController>());
    //   });

    //   test("Set Value", () {
    //     expect(reservaspendentes.value, equals(0));
    //     reservaspendentes.increment();
    //     expect(reservaspendentes.value, equals(1));
    //   });
  });
}
