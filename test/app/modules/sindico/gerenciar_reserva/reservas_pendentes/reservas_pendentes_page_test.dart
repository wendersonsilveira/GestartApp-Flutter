import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:Gestart/app/modules/sindico/gerenciar_reserva/reservas_pendentes/reservas_pendentes_page.dart';

void main() {
  testWidgets('ReservasPendentesPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ReservasPendentesPage(title: 'ReservasPendentes')));
    //  final titleFinder = find.text('ReservasPendentes');
    //  expect(titleFinder, findsOneWidget);
  });
}
