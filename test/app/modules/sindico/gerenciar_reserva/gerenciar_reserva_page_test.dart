import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:Gestart/app/modules/sindico/gerenciar_reserva/gerenciar_reserva_page.dart';

void main() {
  testWidgets('GerenciarReservaPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(GerenciarReservaPage(title: 'GerenciarReserva')));
    //  final titleFinder = find.text('GerenciarReserva');
    //  expect(titleFinder, findsOneWidget);
  });
}
