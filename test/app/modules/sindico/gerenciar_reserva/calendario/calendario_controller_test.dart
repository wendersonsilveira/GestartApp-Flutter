import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:Gestart/app/modules/sindico/gerenciar_reserva/calendario/calendario_controller.dart';
import 'package:Gestart/app/app_module.dart';

void main() {
  initModule(AppModule());
  // CalendarioController calendario;
  //
  setUp(() {
    //     calendario = AppModule.to.get<CalendarioController>();
  });

  group('CalendarioController Test', () {
    //   test("First Test", () {
    //     expect(calendario, isInstanceOf<CalendarioController>());
    //   });

    //   test("Set Value", () {
    //     expect(calendario.value, equals(0));
    //     calendario.increment();
    //     expect(calendario.value, equals(1));
    //   });
  });
}
