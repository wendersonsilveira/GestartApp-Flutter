import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:Gestart/app/modules/sindico/gerenciar_reserva/calendario/calendario_page.dart';

void main() {
  testWidgets('CalendarioPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(CalendarioPage(title: 'Calendario')));
    //  final titleFinder = find.text('Calendario');
    //  expect(titleFinder, findsOneWidget);
  });
}
