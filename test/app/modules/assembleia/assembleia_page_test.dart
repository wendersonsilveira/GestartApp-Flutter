import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:Gestart/app/modules/assembleia/assembleia_page.dart';

void main() {
  testWidgets('AssembleiaPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(AssembleiaPage(title: 'Assembleia')));
    //  final titleFinder = find.text('Assembleia');
    //  expect(titleFinder, findsOneWidget);
  });
}
