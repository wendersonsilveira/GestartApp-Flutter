import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:Gestart/app/modules/assembleia/assembleia_controller.dart';
import 'package:Gestart/app/modules/assembleia/assembleia_module.dart';

void main() {
  initModule(AssembleiaModule());
  // AssembleiaController assembleia;
  //
  setUp(() {
    //     assembleia = AssembleiaModule.to.get<AssembleiaController>();
  });

  group('AssembleiaController Test', () {
    //   test("First Test", () {
    //     expect(assembleia, isInstanceOf<AssembleiaController>());
    //   });

    //   test("Set Value", () {
    //     expect(assembleia.value, equals(0));
    //     assembleia.increment();
    //     expect(assembleia.value, equals(1));
    //   });
  });
}
