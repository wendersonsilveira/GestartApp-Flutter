import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:Gestart/app/modules/condominio/condominio_page.dart';

void main() {
  testWidgets('CondominioPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(CondominioPage(title: 'Condominio')));
    //  final titleFinder = find.text('Condominio');
    //  expect(titleFinder, findsOneWidget);
  });
}
