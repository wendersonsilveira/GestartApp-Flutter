import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:Gestart/app/modules/condominio/condominio_controller.dart';
import 'package:Gestart/app/modules/condominio/condominio_module.dart';

void main() {
  initModule(CondominioModule());
  // CondominioController condominio;
  //
  setUp(() {
    //     condominio = CondominioModule.to.get<CondominioController>();
  });

  group('CondominioController Test', () {
    //   test("First Test", () {
    //     expect(condominio, isInstanceOf<CondominioController>());
    //   });

    //   test("Set Value", () {
    //     expect(condominio.value, equals(0));
    //     condominio.increment();
    //     expect(condominio.value, equals(1));
    //   });
  });
}
