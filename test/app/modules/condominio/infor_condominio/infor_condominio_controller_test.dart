import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:Gestart/app/modules/condominio/infor_condominio/infor_condominio_controller.dart';
import 'package:Gestart/app/modules/condominio/condominio_module.dart';

void main() {
  initModule(CondominioModule());
  // InforCondominioController inforcondominio;
  //
  setUp(() {
    //     inforcondominio = CondominioModule.to.get<InforCondominioController>();
  });

  group('InforCondominioController Test', () {
    //   test("First Test", () {
    //     expect(inforcondominio, isInstanceOf<InforCondominioController>());
    //   });

    //   test("Set Value", () {
    //     expect(inforcondominio.value, equals(0));
    //     inforcondominio.increment();
    //     expect(inforcondominio.value, equals(1));
    //   });
  });
}
