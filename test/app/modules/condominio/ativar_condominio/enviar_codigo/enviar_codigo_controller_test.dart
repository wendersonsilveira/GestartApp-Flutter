import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:Gestart/app/modules/condominio/ativar_condominio/enviar_codigo/enviar_codigo_controller.dart';
import 'package:Gestart/app/modules/condominio/condominio_module.dart';

void main() {
  initModule(CondominioModule());
  // EnviarCodigoController enviarcodigo;
  //
  setUp(() {
    //     enviarcodigo = CondominioModule.to.get<EnviarCodigoController>();
  });

  group('EnviarCodigoController Test', () {
    //   test("First Test", () {
    //     expect(enviarcodigo, isInstanceOf<EnviarCodigoController>());
    //   });

    //   test("Set Value", () {
    //     expect(enviarcodigo.value, equals(0));
    //     enviarcodigo.increment();
    //     expect(enviarcodigo.value, equals(1));
    //   });
  });
}
