import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:Gestart/app/modules/condominio/ativar_condominio/enviar_codigo/enviar_codigo_page.dart';

void main() {
  testWidgets('EnviarCodigoPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(EnviarCodigoPage(title: 'EnviarCodigo')));
    //  final titleFinder = find.text('EnviarCodigo');
    //  expect(titleFinder, findsOneWidget);
  });
}
