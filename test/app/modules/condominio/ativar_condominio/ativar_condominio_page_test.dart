import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:Gestart/app/modules/condominio/ativar_condominio/ativar_condominio_page.dart';

void main() {
  testWidgets('AtivarCondominioPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(AtivarCondominioPage(title: 'AtivarCondominio')));
    //  final titleFinder = find.text('AtivarCondominio');
    //  expect(titleFinder, findsOneWidget);
  });
}
