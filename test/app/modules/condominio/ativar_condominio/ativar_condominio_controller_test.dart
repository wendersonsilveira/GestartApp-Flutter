import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:Gestart/app/modules/condominio/ativar_condominio/ativar_condominio_controller.dart';
import 'package:Gestart/app/modules/condominio/condominio_module.dart';

void main() {
  initModule(CondominioModule());
  // AtivarCondominioController ativarcondominio;
  //
  setUp(() {
    //     ativarcondominio = CondominioModule.to.get<AtivarCondominioController>();
  });

  group('AtivarCondominioController Test', () {
    //   test("First Test", () {
    //     expect(ativarcondominio, isInstanceOf<AtivarCondominioController>());
    //   });

    //   test("Set Value", () {
    //     expect(ativarcondominio.value, equals(0));
    //     ativarcondominio.increment();
    //     expect(ativarcondominio.value, equals(1));
    //   });
  });
}
