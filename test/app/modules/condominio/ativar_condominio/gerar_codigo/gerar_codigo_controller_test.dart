import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:Gestart/app/modules/condominio/ativar_condominio/gerar_codigo/gerar_codigo_controller.dart';
import 'package:Gestart/app/modules/condominio/condominio_module.dart';

void main() {
  initModule(CondominioModule());
  // GerarCodigoController gerarcodigo;
  //
  setUp(() {
    //     gerarcodigo = CondominioModule.to.get<GerarCodigoController>();
  });

  group('GerarCodigoController Test', () {
    //   test("First Test", () {
    //     expect(gerarcodigo, isInstanceOf<GerarCodigoController>());
    //   });

    //   test("Set Value", () {
    //     expect(gerarcodigo.value, equals(0));
    //     gerarcodigo.increment();
    //     expect(gerarcodigo.value, equals(1));
    //   });
  });
}
