import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:Gestart/app/modules/condominio/ativar_condominio/gerar_codigo/gerar_codigo_page.dart';

void main() {
  testWidgets('GerarCodigoPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(GerarCodigoPage(title: 'GerarCodigo')));
    //  final titleFinder = find.text('GerarCodigo');
    //  expect(titleFinder, findsOneWidget);
  });
}
