import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:Gestart/app/modules/boleto/boleto_controller.dart';
import 'package:Gestart/app/modules/boleto/boleto_module.dart';

void main() {
  initModule(BoletoModule());
  // BoletoController boleto;
  //
  setUp(() {
    //     boleto = BoletoModule.to.get<BoletoController>();
  });

  group('BoletoController Test', () {
    //   test("First Test", () {
    //     expect(boleto, isInstanceOf<BoletoController>());
    //   });

    //   test("Set Value", () {
    //     expect(boleto.value, equals(0));
    //     boleto.increment();
    //     expect(boleto.value, equals(1));
    //   });
  });
}
