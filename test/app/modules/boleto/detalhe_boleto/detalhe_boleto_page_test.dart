import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:Gestart/app/modules/boleto/detalhe_boleto/detalhe_boleto_page.dart';

void main() {
  testWidgets('DetalheBoletoPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(DetalheBoletoPage(title: 'DetalheBoleto')));
    //  final titleFinder = find.text('DetalheBoleto');
    //  expect(titleFinder, findsOneWidget);
  });
}
