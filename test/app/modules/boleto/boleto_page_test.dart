import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:Gestart/app/modules/boleto/boleto_page.dart';

void main() {
  testWidgets('BoletoPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(BoletoPage(title: 'Boleto')));
    //  final titleFinder = find.text('Boleto');
    //  expect(titleFinder, findsOneWidget);
  });
}
