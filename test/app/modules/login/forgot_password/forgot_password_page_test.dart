import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:Gestart/app/modules/login/forgot_password/forgot_password_page.dart';

void main() {
  testWidgets('ForgotPasswordPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ForgotPasswordPage(title: 'ForgotPassword')));
    //  final titleFinder = find.text('ForgotPassword');
    //  expect(titleFinder, findsOneWidget);
  });
}
