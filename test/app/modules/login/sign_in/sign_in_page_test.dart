import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:Gestart/app/modules/login/sign_in/sign_in_page.dart';

void main() {
  testWidgets('SignInPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(SignInPage(title: 'SignIn')));
    //  final titleFinder = find.text('SignIn');
    //  expect(titleFinder, findsOneWidget);
  });
}
