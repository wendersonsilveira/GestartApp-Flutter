import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:Gestart/app/modules/login/sign_in/sign_in_controller.dart';
import 'package:Gestart/app/modules/login/login_module.dart';

void main() {
  initModule(LoginModule());
  // SignInController signin;
  //
  setUp(() {
    //     signin = LoginModule.to.get<SignInController>();
  });

  group('SignInController Test', () {
    //   test("First Test", () {
    //     expect(signin, isInstanceOf<SignInController>());
    //   });

    //   test("Set Value", () {
    //     expect(signin.value, equals(0));
    //     signin.increment();
    //     expect(signin.value, equals(1));
    //   });
  });
}
