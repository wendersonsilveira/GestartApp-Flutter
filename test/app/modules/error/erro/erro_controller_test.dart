import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:Gestart/app/modules/error/erro/erro_controller.dart';
import 'package:Gestart/app/app_module.dart';

void main() {
  initModule(AppModule());
  // ErroController erro;
  //
  setUp(() {
    //     erro = AppModule.to.get<ErroController>();
  });

  group('ErroController Test', () {
    //   test("First Test", () {
    //     expect(erro, isInstanceOf<ErroController>());
    //   });

    //   test("Set Value", () {
    //     expect(erro.value, equals(0));
    //     erro.increment();
    //     expect(erro.value, equals(1));
    //   });
  });
}
