import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:Gestart/app/modules/error/erro/erro_page.dart';

void main() {
  testWidgets('ErroPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ErroPage(title: 'Erro')));
    //  final titleFinder = find.text('Erro');
    //  expect(titleFinder, findsOneWidget);
  });
}
